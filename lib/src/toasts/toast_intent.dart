/// Each intent affects the default icon in the title and its colour.
///
/// These icon slots can be overriden to render other content such as progress
/// spinners or avatars.
enum ToastIntent { info, success, warning, error }
