name: fluent_data_grid_example
description: Minimal example for fluent_data_grid
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  fluent_ui: ^4.13.0
  fluent_widgets:
      path: ../../

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: false
