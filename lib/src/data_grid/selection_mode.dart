/// Selection modes for the DataGrid
enum SelectionMode {
  /// No selection allowed
  none,

  /// Single row selection with highlighting
  single,

  /// Multiple row selection with checkboxes
  multi,
}
